import { ReplacecurrencyPipe } from './replacecurrency.pipe';

describe('ReplacecurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplacecurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
